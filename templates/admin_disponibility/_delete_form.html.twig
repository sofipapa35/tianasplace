<form method="post" action="{{ path('admin_disponibility_delete', {'id': disponibility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disponibility.id) }}">
    <button class="btn">Delete</button>
</form>
