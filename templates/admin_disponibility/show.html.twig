{% extends 'base.html.twig' %}

{% block title %}Disponibility{% endblock %}

{% block body %}
    <h1>Disponibility</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disponibility.id }}</td>
            </tr>
            <tr>
                <th>Count</th>
                <td>{{ disponibility.count }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_disponibility_index') }}">back to list</a>

    <a href="{{ path('admin_disponibility_edit', {'id': disponibility.id}) }}">edit</a>

    {{ include('admin_disponibility/_delete_form.html.twig') }}
{% endblock %}
