{% extends 'base-admin.html.twig' %}

{% block title %}Nouvelle Reservation{% endblock %}

{% block body %}
    <h1>Creer une Reservation</h1>

    {{ include('admin_reservation/_form.html.twig') }}

    <a href="{{ path('admin_reservation_index') }}" class="btn btn-secondary">Returner à la liste</a>
{% endblock %}
