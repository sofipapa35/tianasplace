{% extends 'base-admin.html.twig' %}

{% block title %}Jour{% endblock %}

{% block body %}
    <h1>Jour</h1>

    <a href="{{ path('admin_jour_index') }}" class="btn btn-secondary">Retourner à la liste</a> 
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jour.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ jour.jour ? jour.jour|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('admin_jour_edit', {'id': jour.id}) }}" class="btn btn-success"><i class="align-middle" data-feather="edit"></i></a>

    {{ include('admin_jour/_delete_form.html.twig') }}
{% endblock %}
