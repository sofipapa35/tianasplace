<form method="post" action="{{ path('admin_jour_delete', {'id': jour.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jour.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
